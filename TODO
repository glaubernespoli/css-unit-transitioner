
//TODO:
9. auto select values on the inputs when they get focused --OK
2. only numbers on inputs, no negative numbers; -- OK
3. min <= current <= max; --OK
11. if not updating values (due to not being elegible), keep old value --OK
12. set max of min input = current, set min of max input = current --OK
1. initial values of the spans (not X Y Z); --OK
4. update current value on slide; -- OK
5. not trigger change event when using the buttons up and down from the number input 
==>>> FOCUS & ENTER should trigger all the events; change & keyup numbers should only update values -- OK
10. smoothly change the bg on :hover on the unit buttons --OK
6. range silde bar should have the specified color -- OK
7. growth input on top of the range bar to specify how much it should grow (step attr) --OK
8. initial min and max should be based on parent div, not vw (also work with .2 decimals instead of ints when calculating min current and max) --OK
14. error messages if input incorrectly --OK
13. organize the js file - funcions should be in different files based on what they do -- OK
16. style text input -- OK

21. display bars v2 - update every bar to the current value (second tab)

15. readme file
17. responsiveness - input boxes get really bad when in small screen 
==>>> text inside input boxes should be scaled down according to screeen size 
==>>> buttons should align vertically when scaled down
18. refactor updateSpanAndRangeValuesFor() on content-range-input.js - separate tasks
19. Tests DD:
20. adding a space at the end of the bars when using a max value higher than the screen
22. RELATIVE UNITS!!
23. range unit should go all the way to the end, even if current+growth > max (then current should be = max)
24. range unit value should grow relative to the current value in respect to decimals, not to exact numbers 
==>>> (i.e. if current = 36.5 and growth = 1, then if I slide it should go to 35.5/37.5, not 36/38)

BUGS:
1. if you force an error message while the error message div is showing, it behaves stupidly